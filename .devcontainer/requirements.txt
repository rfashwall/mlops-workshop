# Essential packages only - installed during container build
fastapi
uvicorn
requests
pyyaml
kubernetes
prometheus-client
jupyter
jupyterlab

# Heavy ML packages are installed post-creation via scripts/install-ml-packages.sh
# This avoids disk space issues during container build